const sectiontodo = document.querySelector(".todo-section");
console.log(sectiontodo.classList); // show the classes 

// sectiontodo.classList.add('bg-dark')//to add css using javascript we just pas the class name and where to apply like section div
// // sectiontodo.classList.remove('container'); // remove the classes fromk html 
// //to check classexist or not  we can use the below command 
// // console.log(sectiontodo.classList.contains("container"))
//  sectiontodo.classList.toggle(".bg-dark") // if bg-dark class  is not their then it will add else it will 
//  sectiontodo.classList.toogle(".bg-dark"); // if class is their already then remove it else add it 

const header=document.querySelector(".header")
console.log(header.classList)
// header.classList.add("bg-dark");